hi
mov [SecsPerTrack], ax
mov [Sides], bx
mov [bootdev], dl

mov ax, 0x2000
mov es, ax

mov di, skibidi
save_____that______filename:
	lodsb
	cmp al, 0
	je donasd
	mov [es:di], al
	inc di
	jmp save_____that______filename
donasd:

mov di, skibidi
call print

mov ax, 0x2000
mov ds, ax
mov ax, skibidi
call os_file_exists
jc halt


mov cx, 0x3000
call os_load_file

mov ax, 0x3000
mov ds, ax
mov ax, 0x2000
mov es, ax

mov si, 0